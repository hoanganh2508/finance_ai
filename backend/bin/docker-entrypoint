#!/bin/bash -e

# If running the rails server then clean up any stale PID and prepare the DB
if [ "$1" = "./bin/rails" ] && [ "$2" = "server" ]; then
  mkdir -p tmp/pids
  rm -f tmp/pids/server.pid
  ./bin/rails db:prepare
fi

exec "${@}"
